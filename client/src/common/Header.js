import React from 'react'

function Header() {
  return (
    <div className='header  text-center bg-primary text-white'>
        (React+Redux) + Node JS + Mongo DB
    </div>
  )
}

export default Header